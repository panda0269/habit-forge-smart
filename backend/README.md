# Habit Forge Backend

Minimal Node.js + Express backend scaffolding.

## Setup

```bash
cd backend
npm install
npm run dev
```

## Endpoints

- `GET /health` - Returns "API running"

## Notes

- Server runs on port 5000
- No database connection yet
- This backend runs separately from the Lovable frontend
